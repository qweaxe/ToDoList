---
tags:
  - ToDoApp
  - LifeNexus
---
## 1. 项目愿景
构建一个极简、直观且具有高度交互性的待办事项管理应用。系统应支持按天管理任务，并提供月度日历视图以进行宏观规划和数据统计。

## 2. 核心功能需求
### 2.1 任务管理 (CRUD)
- **创建任务**：用户可以在指定日期下新建任务，包含标题和详细描述，任务分类，任务类型（基础任务、跨天任务、多步骤任务、周期任务）和任务等级。
	- 基础任务：只填写dueDate或者startDate == dueDate时满足
	- 跨天任务：一个跨天任务，可以提前完成，跨天任务可以是多步骤任务
	- 多步骤任务：支持主任务+子任务结构，仅有二级分类，主任务完成度由子任务计算百分比得到，可以是基础任务、跨天任务或周期任务
	- 周期任务：可创建重复规则（按照cron表达式），如”每周一买牛奶“
- **展示列表**：按日期分组展示待办事项，区分已完成和未完成状态。
- **状态切换**：一键切换任务的“完成/未完成”状态，完成时要记录完成的日期。
- **编辑功能**：支持内联编辑任务内容（标题、描述、类型、等级）。
- **删除功能**：支持删除单条任务或者批量删除任务，需有二次确认交互。
### 2.2 任务分类管理（CRUD）
- **创建任务分类**：用户可以新建任务分类和对应的描述，以及对应的图标emoji，系统内置emoji，供用户选择
- **展示列表**：展示所有的任务类型设置。
- **编辑功能**：支持内联编辑任务类型内容（类型、描述、图标）。
- **删除功能**：支持删除单个任务类型或者批量删除任务类型，需有二次确认交互，不允许删除已被引用的类型。
### 2.3 任务等级管理（CRUD）
- **创建任务等级**：用户可以新建任务等级。
- **展示列表**：展示所有的任务等级。
- **删除功能**：支持删除单个任务等级或者批量删除任务等级，需有二次确认交互，不允许删除已被引用的等级。
### 2.4 视图维度
- **当日视图 (Day View)**：
    - 显示：`startDate <= 今天 <= endDate` 的所有任务。
    - 日期切换器：支持快速跳转到不同日期，在其他日期时有按钮可以直接回到今天。
    - 任务流：以卡片式布局展示当天的所有任务。
    - 子任务：如果包含子任务，点击卡片展开Checklist，而不是直接勾选完成
    - 空状态：当某天没有任务时，显示优雅的提示。
    - 历史待办任务：显示所有未完成的，`startDate <= 今天 - 1 <= endDate` 的所有任务。任务不包括当天的，按照从最远时间显示到截至前一天的任务，及已经过了结束日期但是没有完成的任务，在任意日期时，待办任务只会显示今天之前未完成的任务，而不会显示今天及以后的待办任务；最多显示5条摘要，按照任务等级排序，超出部分数量提示，并且能点击查看所有为完成历史待办任务明细，明细会单独打开一个子页面显示明细。
- **日历视图 (Calendar View)**：
    - 7x6 标准网格布局，展示整月任务分布，首次进入高亮当天日期，选择后高亮选择的日期。
    - 跨月展示：自动填充上月末尾和下月开头的日期。
    - 单元格预览：每个日期格子最多显示 5 条任务摘要（要考虑网页端和手机端适配的问题），超出部分显示数量提示。
    - 详情弹窗：点击日历单元格或任务摘要，弹出模态框查看/编辑完整详情。
    - 工作日：按照中国公布的法定节假日安排，显示此天是工作日还是休息日；保留可扩展性，支持选择/接入多个国家的法定节假日安排
    - 状态统计：展示当月的任务总量、完成数、未完成数及整体完成率（可视化进度条）。
- **周视图 (Weekly Kanban)：**
	- **展示**：高亮横向排列 7 天的任务列（周一至周日），标题阐明本周是本年的第几周；实现基于 `dnd-kit` 或 `react-beautiful-dnd` 的跨天拖拽功能。
	- **交互**：
	    - **周总结**：顶部显示本周完成率及重点任务摘要。
- **UI**：使用紧凑型卡片，突出显示任务优先级。
- **季度视图 (Quarterly Roadmap)：**
	- **展示**：将季度拆分为 3 个月，以进度条形式展示大任务或重复性项目的覆盖周期；不要展示所有任务细节，仅提取 `isMilestone` 为 true 的任务。
	- **重点**：侧重于“截止日期（Deadlines）”和“重要里程碑”。
	- **统计**：显示季度目标完成进度（OKR 追踪）。
- **年度视图 (Yearly Footprints)：**
	- **展示**：使用一个轻量级的 CSS Grid 构建热力图，并实现点击跳转。
	    - **全景热力图**：参考 GitHub 贡献图，展示一年 365 天的任务完成密度。
	    - **年度统计**：总计完成任务数、最勤奋的月份、最专注的类别。
	- **交互**：悬停显示当天摘要，点击跳转至当日详情。
### 2.5 设置管理
- **任务类型管理**：
	- 显示所有的任务类型，让用户在此页面进行管理
- **任务等级管理**：
	- 显示所有的任务等级，让用户在此页面进行管理
### 2.6 交互体验 (UX)
- **响应式设计**：适配移动端和桌面端。
- **即时反馈**：添加/删除/状态切换需有平滑的动画过渡（Transition）。
- **加载状态**：全局 Loading 状态及按钮级别的处理状态（updating/loading）。
- **错误处理**：完善的 API 报错提示（Toast 或错误信息条）。
- **性能优化**：年度视图数据量大，请通过 tRPC 的特定接口只获取计数统计，不要拉取完整的任务对象。"

## 3. 技术约束 (Stack)
_请 AI 严格按照以下技术栈实现：_
- **MonoRepo**: TurboRepo
- **运行时**: Bun
- **前端**: React (Next.js 或 Vite) + TanStack Query (React Query)
- **后端**: Hono (部署在 Cloudflare Workers 或 Vercel)
- **通信**: tRPC (实现端到端类型安全)
- **样式**: Tailwind CSS (实现渐变背景和卡片布局)
- **语言**: TypeScript (严格模式)

## 4. 界面原型规范 (UI)
- **配色方案**：
    - 背景：现代感渐变色（如 `from-blue-500 to-indigo-600`）。
    - 卡片：半透明毛玻璃效果（Glassmorphism）或纯白高光卡片。
    - 文字：深灰色（主文本）及中灰色（描述文本）。
- **布局**：
    - 侧边栏/顶部导航：支持在“日历”与“今日”视图间切换。
    - 日历格子：
        - 今天：高亮边框/背景。
        - 周末：日期数字红色标识。
        - 已完成任务：文字中划线 + 灰色。
## 5. 数据模型 (Schema)

``` TypeScript
interface Todo {
  id: string;
  title: string;
  description?: string;
  status: 'pending' | 'in_progress' |'completed';
  
  //时间维度
  startDate: string;//
  dueDate: string; // ISO格式: YYYY-MM-DD
  
  //进度、多步骤
  subTasks: {
	  id: string;
	  text: string;
	  isDone: boolean;
  }[];
  //是否周期任务
  isCycleTask: boolean;
  
  createdAt: Date;
  updatedAt: Date;
  priority: int; // 新增：用于区分任务的优先级，默认值为0
  taskType: string; // 新增：用于区分任务的类型，默认值为“默认”
  isMilestone: boolean; // 新增：是否为里程碑（在季度视图突出显示）
}

interface RecurrenceRule{
	id: string;
	frequency: string;
	interval: number;
	byDay?: number[];
	startDate: string; 
	endDate: string;
}
```

## 6. API 接口定义 (tRPC Router)
- `todo.getDaily(date: string)`: 获取指定日期的列表。
- `todo.getMonthly(year: number, month: number)`: 获取整月数据。
- `todo.create(data: CreateTodoInput)`: 创建。
- `todo.toggle(id: string)`: 切换状态。
- `todo.update(id: string, data: UpdateTodoInput)`: 更新内容。
- `todo.delete(id: string)`: 删除。
- `todo.deleteBatch(ids: string[])`：批量删除
- `todo.getWeekly(startDate: string)`: 获取一周的数据。
- `todo.getQuarterly(StartDate: string)`: 获取一个季度的大任务或重复性项目的数据。
- `todo.getYearlyStats(year: number)`: 获取年度热力图统计数据（仅返回日期和完成数计数，优化性能）。
## 7. 备注
1. 在 **TurboRepo** 里的 `packages/utils` 中，专门写一个 `date-fns` 的封装库，用来统一处理周、月、季度的日期计算。这样能确保在前端 React 计算日历网格和后端 Hono 查询数据库时，对“这一周”或“这一季”的定义是完全对齐的。
2. 幂等逻辑：在`todo.getDaily` 或 `todo.getWeekly` 被调用时，后端 Hono 首先执行一个 `SyncService`。该服务根据 `RecurrenceRule` 计算当前时间窗口内应存在哪些任务，若缺失则静默创建。这确保了用户无论何时查看日历，数据都是完整的。
3. 性能策略：年度热力图接口 `todo.getYearlyStats` 必须**排除**处于 `skipped` 状态的任务，且对跨天任务仅在其最终 `done` 的日期进行打点计数。